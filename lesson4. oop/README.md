# Lesson 4. Объектно-ориентированное программрование

Автор: Кивилев Денис (kivilev.d@gmail.com)  
Школа: backend-pro.ru  

### Оглавление
* [Материалы к уроку](#материалы-к-уроку)
* [СКВОЗНАЯ РАБОТА № 4](#homework)


## Материалы к уроку

### Полезные ссылки
- [Класс `java.lang.Object` (Java 21)](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Object.html)  
  Базовый класс и корень иерархии типов Java: контракт `equals/hashCode/toString`, `clone`, `finalize` (устаревший), синхронизация (`wait/notify`), роль в ООП и коллекциях.

- [Утилитный класс `java.util.Objects` (Java 21)](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/Objects.html)  
  Набор статических методов для безопасной работы с объектами: `requireNonNull`, `equals`, `hash`, `compare`, удобная реализация `toString`, помощь в реализации контрактов сущностей.

- [Класс `java.lang.reflect.Constructor` (Java 21)](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/reflect/Constructor.html)  
  Работа с конструкторами через рефлексию: поиск и вызов конструкторов, создание объектов динамически, обработка модификаторов доступа и исключений.

- [Пакет `java.lang` (Java 21)](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/package-summary.html)  
  Обзор базовых классов платформы: примитивные обёртки, строки, системные утилиты, потоки, исключения и прочие фундаментальные компоненты Java.

- [Полная JDK 21 API-документация](https://docs.oracle.com/en/java/javase/21/docs/api/allclasses-index.html)  
  Сводный список всех пакетов и классов стандартной библиотеки JDK 21, удобная точка входа для навигации по API.

- [Обзор ООП и Java 21](https://docs.oracle.com/en/java/javase/21/)  
  Главная страница документации Java SE 21: overview, гайды, туториалы и ссылки на спецификации и API.

- [Принципы ООП на Java (русскоязычное объяснение)](https://javarush.com/groups/posts/1966-principih-obhhektno-orientirovannogo-programmirovanija)  
  Интуитивное объяснение инкапсуляции, наследования, полиморфизма и абстракции с привязкой к Java-коду и практическим примерам.

- [Наследование в Java](https://javarush.com/quests/lectures/questsyntaxpro.level07.lecture06)  
  Подробный разбор ключевого механизма ООП: иерархии классов, ключевое слово `extends`, переопределение методов и типичные ошибки новичков.

- [Полиморфизм в Java](https://habr.com/ru/articles/910194/)  
  Практическое понимание полиморфизма, позднего связывания, динамической диспетчеризации методов и их влияния на дизайн архитектуры.

- [Классы и объекты в официальном туториале Java](https://docs.oracle.com/javase/tutorial/java/javaOO/index.html)  
  Базовый курс по объектно-ориентированному программированию на Java: объявление классов, объекты, поля, методы, конструкторы и инкапсуляция.


## <a name="homework"></a> 🏠 СКВОЗНАЯ РАБОТА № 4


### Задание: Сущности платёжной системы (классы, инкапсуляция, конструкторы)

В рамках сквозного проекта «Платёжная система» закрепите материал лекции 4 по ООП: создайте пакет сущностей, подключите готовые классы и **самостоятельно с нуля** реализуйте три класса доменной модели — `Payment`, `PaymentDataField`, `PaymentDetail`. Затем в методе `main` создайте объекты этих классов и выведите их в консоль для проверки.

**Что нужно сделать:**

1. В своём сквозном проекте платёжной системы создать пакет `entities` (папка `entities` в пакете вашего приложения, например `ru.backendpro.entities`).
2. Скопировать в этот пакет уже готовые классы из материалов курса: `Account`, `Client`, `ClientData`, `ClientDataField`, `Currency`, `ValidationUtils` (папка homework4-classes)
3. **С нуля** написать три класса:
   - **Payment** — платёж (ид, дата, счёт-источник, счёт-получатель, сумма, валюта, статус, дата обработки).
   - **PaymentDataField** — справочник полей платёжных данных (ид поля, название, описание).
   - **PaymentDetail** — значение поля платёжных данных по конкретному платежу (ид, платёж, поле, значение).
4. Очистить содержимое метода `main` (psvm) от лишнего кода и оставить только точку входа.
5. В методе `main` для тренировки создать объекты: клиент, счета, платёж, поле платёжных данных, детализацию платёжа — и вывести их в консоль (например, через `System.out.println(object)`).

---

### Требования

- **Поля:** все поля классов должны иметь модификатор доступа `private`. Для полей, которые не должны меняться после создания объекта (идентификаторы, дата создания, счета в платеже и т.п.), использовать `final`.
- **Конструкторы:** у каждого класса — хотя бы один параметризованный конструктор. Для `Payment` желательно два: полный и сокращённый (например, со статусом по умолчанию `"PENDING"`), с делегированием через `this(...)`.
- **Геттеры и сеттеры:** для чтения полей — геттеры (`getX()`, для `boolean` при необходимости `isX()`). Сеттеры — только у тех полей, которые по смыслу могут изменяться после создания (например, `status`, `processedAt`, `fieldValue`). У immutable-полей (например, `id`, ссылки на счета) — только геттеры.
- **Валидация:** в конструкторах и сеттерах проверять входные данные (не `null`, не пустые строки где недопустимо, положительность суммы/ид при необходимости). Можно использовать утилитный класс `ValidationUtils` по аналогии с `Account` или написать простые проверки.
- **toString:** во всех трёх классах переопределить `toString()` для удобного вывода в консоль и отладки (формат на ваше усмотрение, например `"Payment{id=1, ...}"`).
- **Пакет:** классы должны находиться в пакете `entities` вашего проекта.

---

### Пример вывода программы

Ниже — пример того, как может выглядеть вывод после создания объектов в `main` (конкретный формат строк зависит от вашей реализации `toString()`).

```
Payment{id=1, paymentDate='2025-01-29', sourceAccount=Account{id=1, ...}, targetAccount=Account{id=2, ...}, amount=500.0, currencyCode='RUB', status='PENDING', processedAt='null'}

PaymentDataField{fieldId=1, name='CARD_MASK', description='Masked card number'}

PaymentDetail{id=1, payment=Payment{id=1, ...}, field=PaymentDataField{fieldId=1, ...}, fieldValue='4276****1234'}
```

Допускается более краткий или более подробный вывод — важно, чтобы были видны ключевые поля созданных объектов.

---

### 🎓 Методические советы студенту

- Вспомните аналогию из лекции: **класс — чертёж, объект — экземпляр в памяти**. Сначала продумайте, какие поля нужны сущности, какие из них не меняются (`final`), какие — меняются (для них понадобятся сеттеры).
- **Payment** связывает два счета (`Account`): источник и получатель. Типы полей — как в уже скопированном классе `Account` (например, `Long id`, `double amount`, `String` для дат и кода валюты).
- **PaymentDataField** по структуре похож на **ClientDataField**: идентификатор поля, название, описание. Можно ориентироваться на уже скопированный класс.
- **PaymentDetail** связывает платёж (`Payment`), поле справочника (`PaymentDataField`) и значение (`String`). По смыслу аналогичен **ClientData** (клиент + поле + значение).
- Конструктор с большим числом параметров можно упростить: сделать «главный» конструктор и второй, который вызывает его через `this(...)` с значениями по умолчанию (например, статус `"PENDING"`).
- В сеттерах и конструкторах вызывайте проверки до присваивания: так вы соблюдаете инкапсуляцию и не допускаете некорректного состояния объекта.
- Для вывода в консоль достаточно передать объект в `System.out.println(...)` — будет вызван ваш `toString()`.

---

### Критерии оценки

1. **Структура проекта:** создан пакет `entities`, в нём присутствуют скопированные классы `Account`, `Client`, `ClientData`, `ClientDataField` и самостоятельно написанные `Payment`, `PaymentDataField`, `PaymentDetail`.
2. **Инкапсуляция:** поля объявлены как `private`; для неизменяемых данных использованы `final` и только геттеры; для изменяемых полей — геттеры и при необходимости сеттеры с проверками.
3. **Конструкторы:** у каждого из трёх новых классов есть рабочий параметризованный конструктор; у `Payment` — перегрузка и делегирование через `this(...)` (по возможности).
4. **Валидация:** в конструкторах и/или сеттерах выполняются проверки (не null, не пустые строки, положительность где нужно); некорректные значения приводят к понятному исключению.
5. **toString:** во всех трёх классах переопределён `toString()`, вывод читаемый и содержит основные поля объекта.
6. **main:** в методе `main` созданы объекты сущностей (в т.ч. клиент, счета, платёж, поле платёжных данных, детализация) и выполнен их вывод в консоль; программа компилируется и выполняется без ошибок.
