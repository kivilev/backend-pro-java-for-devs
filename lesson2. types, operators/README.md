# Lesson 2. Основы синтаксиса Java

Автор: Кивилев Денис (kivilev.d@gmail.com)  
Школа: backend-pro.ru  

### Оглавление
* [Материалы к уроку](#материалы-к-уроку)
* [LIVE CODING БЛОК 1. Типы](#livecode1)
* [LIVE CODING БЛОК 2. Калькулятор](#livecode2)
* [ДОМАШНЕЕ ЗАДАНИЕ (сквозная работа)](#homework)


## Материалы к уроку

### Полезные ссылки
Oracle Java Tutorials:
- Primitive Data Types -- https://docs.oracle.com/javase/tutorial/java/nutsandbolts/datatypes.html  
- Operators - https://docs.oracle.com/javase/tutorial/java/nutsandbolts/operators.html  
- Control Flow - https://docs.oracle.com/javase/tutorial/java/nutsandbolts/flow.html  

Modern Java Features:
- Local Variable Type Inference (var) - https://openjdk.org/jeps/286  
- Switch Expressions - https://openjdk.org/jeps/361


## <a name="livecode1"></a> 💻 LIVE CODING БЛОК 1. Типы Java

Задания сфокусированы на **синтаксисе, типах данных и понимании памяти**, но обернуты в контекст **финансового процессинга**.

### Задание 1. Инициализация транзакции (Примитивы)
**Цель:** Отработать объявление примитивных типов.
**Задача:** В методе `main` объявите переменные для хранения данных одного платежа:
*   ID транзакции (целое число, до 2 млрд)
*   Сумма в центах (целое число, может быть очень большим)
*   Код валюты (числовой код ISO, например 840)
*   Статус (логическое значение: проведен/не проведен)
*   Рейтинг надежности клиента (символ: A, B, C)
**Контекст:** Используйте `int`, `long`, `short`, `boolean`, `char`.

### Задание 2. Константы платежной системы (final)
**Цель:** Понять неизменяемость данных.
**Задача:** Создайте переменные-константы для настроек системы:
*   Название платежного шлюза (String)
*   Максимальная сумма разового перевода
*   Комиссия системы (дробное число)
**Контекст:** Попробуйте изменить значение константы после инициализации и посмотрите на ошибку компилятора.

### Задание 3. Читаемость больших чисел (Underscore)
**Цель:** Изучить синтаксический сахар для работы с суммами.
**Задача:** Объявите переменную `totalVolume` (общий объем транзакций за день), равную 10 миллиардам 500 миллионам. Используйте нижнее подчеркивание для разделения разрядов, чтобы число было легко читать. Не забудьте про суффикс `L`.

### Задание 4. Объектная обертка для суммы (Wrapper classes)
**Цель:** Увидеть разницу между примитивом и объектом.
**Задача:** Представьте, что сумма платежа может быть не указана (null). 
*   Создайте переменную `amount` типа `Double`. Присвойте ей `null`.
*   Создайте переменную `primitiveAmount` типа `double`. Попробуйте присвоить ей `null`.
*   Запишите в комментариях, почему в первом случае это сработало, а во втором — нет.

### Задание 5. Опасный Unboxing (NPE)
**Цель:** Поймать свой первый NullPointerException и понять его причину.
**Задача:** 
1. Создайте `Integer transactionCount = null;`.
2. Попробуйте присвоить это значение примитиву: `int count = transactionCount;`.
3. Запустите программу. Разберитесь, почему возникла ошибка (подсказка из PDF: автоматическая распаковка).

### Задание 6. Работа с именами (String & Immutability)
**Цель:** Понять, что строки не меняются "внутри".
**Задача:** 
1. Создайте строку `clientName = "ivan"`.
2. Вызовите метод `clientName.toUpperCase()`.
3. Выведите `clientName` на экран. Почему имя осталось маленькими буквами?
4. Исправьте код так, чтобы результат метода сохранился в новую переменную.

### Задание 7. Лаконичный код с var (Java 10+)
**Цель:** Отработать локальный вывод типов.
**Задача:** Перепишите объявление следующих переменных, используя `var`:
*   `String provider = "Stripe";`
*   `Double balance = 500.50;`
*   `Integer retryCount = 3;`
**Контекст:** Обсудите, в каких случаях `var` делает код понятнее, а в каких — запутывает.

### Задание 8. Точность расчетов (Float vs Double)
**Цель:** Увидеть разницу в точности дробных чисел.
**Задача:** 
1. Объявите `float fCommission = 0.123456789f;`
2. Объявите `double dCommission = 0.123456789;`
3. Выведите оба значения. Посмотрите, сколько знаков после запятой сохранилось в каждом случае. Для финансовых систем это критично!

### Задание 9. Стек и Куча (Теоретическое моделирование)
**Цель:** Закрепить понимание структуры памяти.
**Задача:** Нарисуйте (на бумаге или в текстовом виде) схему памяти для следующего кода:
```java
int limit = 1000;
String currency = "USD";
Double rate = 1.1;
```
**Вопрос:** Какие из этих данных окажутся полностью в Стеке, а какие — создадут объект в Куче (Heap)?

---
**Совет:** При выполнении заданий в IntelliJ IDEA обращайте внимание на цвет переменных и подсказки (инспекции) среды разработки — она часто подсказывает, где можно использовать `var` или где может возникнуть `NullPointerException`.


## <a name="livecode2"></a> 💻 LIVE CODING БЛОК 2. Операторы

Продолжаем обучение. Вторая часть лекции №2 посвящена логике управления программой. Для студента это критический этап: здесь он учится "заставлять" программу принимать решения.

Как Senior Architect, я подготовил 10 заданий, которые моделируют логику реального платежного движка.

### Практический блок: Операторы и логика в финансовом процессинге

#### Задание 1. Расчет комиссии (Арифметические операторы)
**Цель:** Понять особенности целочисленного деления.
**Задача:** 
1. Объявите `int amount = 1000;` (сумма в рублях).
2. Рассчитайте комиссию 3%. Сначала сделайте это через `int commission = amount * 3 / 100;`.
3. Затем попробуйте `double commissionDouble = amount * (3 / 100);`. 
4. Выведите оба результата. Объясните, почему во втором случае комиссия стала равна 0.0.

#### Задание 2. Проверка лимита (Операторы сравнения)
**Цель:** Отработать базовые условия.
**Задача:** Напишите программу, которая проверяет, проходит ли платеж по лимитам.
*   Объявите `int paymentAmount = 150000;`
*   Объявите `int maxLimit = 100000;`
*   Выведите результат сравнения: `boolean isAllowed = ...;`

#### Задание 3. Безопасная проверка (Короткое замыкание `&&`)
**Цель:** Понять важность порядка условий для предотвращения ошибок.
**Задача:** 
1. Создайте `String paymentStatus = null;`.
2. Напишите условие `if (paymentStatus != null && paymentStatus.equals("SUCCESS"))`.
3. Поменяйте условия местами: `if (paymentStatus.equals("SUCCESS") && paymentStatus != null)`.
4. Запустите оба варианта. Объясните, почему второй вариант "падает" с ошибкой, а первый — нет.

#### Задание 4. Сравнение валют (equals vs ==)
**Цель:** Закрепить разницу между сравнением ссылок и содержимого.
**Задача:** 
1. Создайте две строки: `String currency1 = new String("USD");` и `String currency2 = new String("USD");`.
2. Сравните их через `==` и через `.equals()`.
3. Выведите результаты. Это классическая ловушка в Java-интервью.

#### Задание 5. Конвертация типов (Casting)
**Цель:** Отработать явное приведение типов.
**Задача:** 
1. У вас есть баланс в `double balance = 500.99;`.
2. Система работает только с целыми числами (целые рубли). Приведите баланс к `int`.
3. Выведите результат. Что произошло с копейками?

#### Задание 6. Категория клиента (if-else-if)
**Цель:** Построение сложной логики ветвления.
**Задача:** Напишите классификатор клиентов по сумме оборота:
*   До 10 000 — "Bronze"
*   От 10 000 до 100 000 — "Silver"
*   Выше 100 000 — "Gold"
**Контекст:** Используйте конструкцию `if-else if-else`.

#### Задание 7. Статус платежа (Switch Expression)
**Цель:** Освоить современный синтаксис Java 14+.
**Задача:** Переведите код ошибки в понятное сообщение, используя **новый** `switch` (со стрелками `->`):
*   Код 1 -> "Успешно"
*   Код 2 -> "Отклонено банком"
*   Код 3 -> "Недостаточно средств"
*   Остальное -> "Техническая ошибка"  

При выполнении попробуйте специально не указывать `default` и посмотрите, как компилятор Java заставит его обработать все возможные варианты.

#### Задание 8. Начисление кэшбэка (Цикл for)
**Цель:** Отработать обход массива данных.
**Задача:** 
1. Создайте массив последних пяти платежей: `int[] payments = {100, 500, 1000, 50, 200};`.
2. Используя цикл `for-each`, рассчитайте общую сумму кэшбэка (допустим, 1% от каждого платежа).
3. Выведите итоговую сумму кэшбэка.

#### Задание 9. Ожидание подтверждения (Цикл while)
**Цель:** Понять работу циклов с условием.
**Задача:** Моделируем ожидание ответа от банковского шлюза.
1. Создайте переменную `int attempts = 0;`.
2. Используя цикл `while`, увеличивайте `attempts`, пока их количество меньше 5.
3. На каждой итерации выводите: "Ожидание ответа... Попытка №".

#### Задание 10. Поиск подозрительного платежа (break)
**Цель:** Использование операторов прерывания цикла.
**Задача:** 
1. Дан массив сумм: `{500, 1200, 15000, 800, 2000}`.
2. Пройдите по массиву циклом `for`.
3. Если сумма платежа превышает 10 000, выведите "Подозрительный платеж найден!" и немедленно **прервите** цикл (используйте `break`).


## <a name="homework"></a> 🏠 СКВОЗНАЯ РАБОТА № 2

#### Задание: Реализовать расчет комиссии для нескольких платежей в методе `main`

#### Описание
На лекции №2 вы изучили: **примитивные и ссылочные типы, `String`, операторы, условия (`if-else`, тернарный оператор), новый `switch expression` (Java 14+), циклы (`for`, `while`, `do-while`), операторы `break` и `continue`**.

В этом задании вы расширите программу `PaymentProcessingApplication`. Вместо простого вывода текста, программа должна обработать несколько платежей: рассчитать комиссию в зависимости от валюты и статуса, пропустить некорректные платежи и вывести итоговый отчет в консоль.

**Всё реализуется внутри метода `main`** — без создания дополнительных методов или классов.

#### Требования

##### 1. Объявление переменных для платежей
В методе `main` объявите переменные для **5 платежей**. Для каждого платежа нужны:
- `тип amount` — сумма в копейках/центах (например, `150000` = 1500 рублей)
- `тип currency` — валюта (`"RUB"`, `"USD"`, `"EUR"`)
- `тип status` — статус платежа (`"NEW"`, `"COMPLETED"`, `"FAILED"`)
- `тип clientName` — имя клиента

Пример для первого платежа:
```java
тип amount1 = 150000; // 1500 рублей
тип currency1 = "RUB";
тип status1 = "NEW";
тип clientName1 = "Иван Иванов";
```

Создайте аналогичные переменные для платежей 2, 3, 4, 5 с разными значениями:
- Минимум один платеж со статусом `"FAILED"` (для демонстрации `continue`)
- Минимум один платеж с `amount = 0` (для демонстрации `break`)
- Разные валюты (`RUB`, `USD`, `EUR`)

##### 2. Константы для комиссий
Объявите константы **перед** циклом (но внутри `main`):
```java
// нужно указать тип и модификатор константы
FEE_PERCENT_RUB = 1;
FEE_PERCENT_USD = 2;
FEE_PERCENT_EUR = 2;
MIN_FEE_RUB = 100;
MIN_FEE_USD = 50;
MIN_FEE_EUR = 50;
```

##### 3. Обработка платежей в цикле `for`
Создайте цикл `for` на 5 итераций. На каждой итерации:

1. **Выбор данных платежа** — используйте `if` или `switch` по номеру итерации, чтобы "загрузить" нужные переменные:
```java
for (int i = 1; i <= 5; i++) {
    long amount;
    String currency;
    String status;
    String clientName;
    
    if (i == 1) {
        amount = amount1;
        currency = currency1;
        status = status1;
        clientName = clientName1;
    } else if (i == 2) {
        // ... данные платежа 2
    }
    // и так далее
```

2. **Проверка на `null`** — если `currency равен null` или `clientName равен null`, вывести `"Ошибка: некорректные данные для платежа " + i` и использовать `continue`.

3. **Пропуск `FAILED`** — если `status равен "FAILED"`, вывести `"Платеж " + i + " отклонен"` и использовать `continue`.

4. **Остановка при нулевой сумме** — если `amount равен 0`, вывести `"Обнаружен платеж с нулевой суммой. Остановка обработки."` и использовать `break`.

5. **Расчет комиссии** — используйте **новый `switch expression`**:
```java
тип feePercent = switch (currency) {
    если "RUB" -> FEE_PERCENT_RUB;
    если "USD" -> FEE_PERCENT_USD;
    если "EUR" -> FEE_PERCENT_EUR;
    по умолчанию -> 0;
};
```

Затем рассчитайте комиссию (только для статуса `"NEW"`):
```java
long fee = 0;
if (status.equals("NEW")) {
    fee = amount * feePercent / 100;
    
    // Проверка минимальной комиссии
    long minFee = switch (currency) {
        если "RUB" -> MIN_FEE_RUB;
        если "USD" -> MIN_FEE_USD;
        если "EUR" -> MIN_FEE_EUR;
        по умолчанию -> 0L;
    };
    
    if (fee < minFee) {
        fee = minFee;
    }
}
```

6. **Вывод результата**:
```java
System.out.println("Платеж " + i + ": клиент=" + clientName + 
    ", сумма=" + amount + " " + currency + 
    ", статус=" + status + ", комиссия=" + fee);
```

##### 4. Использование `var`
Используйте `var` минимум в **2 местах**, где тип очевиден.

##### 5. Цикл `while` для итогового сообщения
После цикла `for` добавьте переменную-счетчик успешно обработанных платежей. Используйте цикл `while`, чтобы вывести сообщение `"Обработка завершена"` один раз:
```java
int counter = 1;
while (counter <= 1) {
    System.out.println("=== Обработка завершена ===");
    counter++;
}
```

##### 6. Демонстрация опасности `NullPointerException` (закомментировано)
В конце `main` добавьте **закомментированный** блок кода, демонстрирующий проболему с NPE.

##### 7. Качество кода
- Запустите `mvn clean compile` — код должен компилироваться.
- Запустите `mvn checkstyle:check` — исправьте все замечания.
- Соблюдайте naming conventions (camelCase для переменных, UPPER_CASE для констант).

#### Критерии оценки
- ✅ Все переменные объявлены в `main`, используются примитивные типы (`long`, `int`) и `String`
- ✅ Реализован `switch expression` с оператором `->` для расчета комиссии
- ✅ Применены циклы `for` и `while`
- ✅ Использованы операторы `break` и `continue` по назначению
- ✅ Есть проверка на `null` через `if (var == null)`
- ✅ Использовано ключевое слово `var` минимум в 2 местах
- ✅ Программа выводит корректные результаты в консоль
- ✅ Код проходит `mvn checkstyle:check` без ошибок
- ✅ Изменения оформлены в отдельном PR в GitHub

