# Lesson 3. Строки и массивы

Автор: Кивилев Денис (kivilev.d@gmail.com)  
Школа: backend-pro.ru  

### Оглавление
* [Материалы к уроку](#материалы-к-уроку)
* [LIVE CODING БЛОК 1. Типы](#livecode1)
* [LIVE CODING БЛОК 2. Операторы](#livecode2)
* [СКВОЗНАЯ РАБОТА № 3](#homework)


## Материалы к уроку

### Полезные ссылки
- **Класс `String` (`java.lang.String`)**  
  [Класс `java.lang.String`](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/String.html)  
  Документация по неизменяемым строкам: основные операции, сравнение, поиск/замена, кодировки, производительность и контракты методов.

- **Класс `StringBuilder` (`java.lang.StringBuilder`)**  
  [Класс `java.lang.StringBuilder`](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/StringBuilder.html)  
  Изменяемая последовательность символов для эффективной конкатенации в одном потоке (в отличие от `StringBuffer`), основные методы вставки/удаления/append.

- **Класс `Arrays` (`java.util.Arrays`)**  
  [`java.util.Arrays`](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/Arrays.html)  
  Утилиты для массивов: сортировка, бинарный поиск, сравнение, заполнение, копирование, стримы из массивов, `equals/deepEquals`, `hashCode/deepHashCode`.

- **Обработка строк в Java: `String`, `StringBuffer`, `StringBuilder`**  
  [Habr: String vs StringBuffer vs StringBuilder](https://habr.com/ru/articles/260767/)  
  Практическое сравнение строковых типов, когда какой выбирать, влияние неизменяемости `String` и стоимость конкатенации.

- **Внутреннее устройство строк в Java**  
  [Habr: внутренности `String`](https://habr.com/ru/articles/488874/)  
  Разбор представления строк внутри JVM, оптимизаций и нюансов, которые влияют на память и производительность.

- **`Arrays` Class в Java (обзор методов)**  
  [Scaler: Arrays class in Java](https://www.scaler.com/topics/java/arrays-class-in-java/)  
  Обзор ключевых методов `Arrays` с примерами: сортировка, поиск, копирование, сравнение массивов.

- **Varargs и массивы в анализе кода**  
  [Habr (PVS-Studio): varargs и массивы](https://habr.com/ru/companies/pvs-studio/articles/688422/)  
  Подводные камни `varargs`, как это превращается в массивы, типичные ошибки и что видит статический анализатор.


## <a name="livecode1"></a> 💻 LIVE CODING. ЧАСТЬ 1. Строки

### Задание 1. Сравнение статуса платежа: `==` vs `equals()`
**Цель:** закрепить правильное сравнение строк.  
**Задача:** Даны:
- `String statusFromApi = new String("COMPLETED");`
- `String expected = "COMPLETED";`

Сделайте 3 проверки и выведите результаты:
1) `statusFromApi == expected`  
2) `statusFromApi.equals(expected)`  
3) `"COMPLETED".equals(statusFromApi)`  

**Контекст:** статус платежа пришёл из внешней системы (объект создан не как литерал).

---

### Задание 2. String Pool на примере кодов валют
**Цель:** понять переиспользование литералов в пуле строк.  
**Задача:** Создайте переменные:
- `String c1 = "USD";`
- `String c2 = "USD";`
- `String c3 = new String("USD");`

Выведите:
- `c1 == c2`
- `c1 == c3`
- `c1.equals(c3)`

**Контекст:** в процессинге коды валют часто повторяются — важно понимать, что такое пул строк и почему `==` опасен.

---

### Задание 3. Иммутабельность: «обновим назначение платежа»
**Цель:** увидеть, что `String` не меняется «на месте».  
**Задача:**  
1) `String purpose = "PAYMENT";`  
2) Сделайте `purpose.concat("_REFUND");` (результат никуда не сохраняйте)  
3) Выведите `purpose`  
4) Затем создайте `String newPurpose = purpose.concat("_REFUND");` и выведите обе строки

**Контекст:** вы формируете назначение операции: `PAYMENT`, `PAYMENT_REFUND`, и т.д.

---

### Задание 4. Проверка «пустого» поля: `isEmpty()` vs `isBlank()`
**Цель:** научиться валидировать строковые поля.  
**Задача:** Даны строки:
- `String comment1 = "";`
- `String comment2 = "   ";`
- `String comment3 = "OK";`

Для каждой выведите результаты `isEmpty()` и `isBlank()`.

**Контекст:** комментарий к платежу может быть пустым, а может содержать только пробелы — это разные ситуации.

---

### Задание 5. Нормализация входных данных: `trim()`/`strip()` + `toUpperCase()`
**Цель:** отработать чистку и нормализацию строк.  
**Задача:** Дано:
- `String rawCurrency = "  uSd  ";`

Сделайте:
1) Удаление пробелов по краям (`trim()` или `strip()`)  
2) Приведение к верхнему регистру  
3) Выведите итог: должно получиться `USD`

**Контекст:** код валюты пришёл от пользователя/партнёра в «грязном» виде.

---

### Задание 6. Разбор простой строки платежа через `split(",")`
**Цель:** научиться делить строку на поля и доставать значения по индексу.  
**Задача:** Дано:
- `String csv = "TX1001,12500,USD,COMPLETED";`

Разбейте на массив `parts = csv.split(",");` и выведите:
- transactionId (элемент 0)
- amountInCents (элемент 1)
- currency (элемент 2)
- status (элемент 3)

**Контекст:** процессинг часто получает данные пачками в CSV-подобном формате.

---

### Задание 7. Ловушка regex в `split`: разделитель `"."`
**Цель:** закрепить мысль «`split()` — это regex».  
**Задача:** Дано:
- `String merchantCode = "shop.eu.pay";`

1) Попробуйте `merchantCode.split(".")` и выведите длину массива  
2) Исправьте на `merchantCode.split("\\.")` и выведите элементы массива

**Контекст:** идентификаторы мерчанта/подсистем могут быть точечными: `shop.eu.pay`.

---

### Задание 8. Форматирование чека: `String.format()` (сумма и валюта)
**Цель:** отработать форматирование без конкатенации.  
**Задача:** Дано:
- `String txId = "TX2002";`
- `double amount = 1234.5;`
- `String currency = "EUR";`

Сформируйте строку вида:
`Payment TX2002: 1234.50 EUR`

Используйте `String.format(...)` (или `.formatted(...)`).

---

### Задание 9. Сбор лог-строки в цикле: `StringBuilder`
**Цель:** правильно собирать строку в цикле без `+`.  
**Задача:** Есть массив статусов по шагам процессинга:
- `String[] steps = {"INIT", "AUTH", "CAPTURE", "COMPLETED"};`

Соберите одну строку:
`INIT -> AUTH -> CAPTURE -> COMPLETED`

Требования:
- используйте `StringBuilder`
- в конце не должно быть лишнего `->`

---

### Задание 10. Text Blocks: JSON-запрос на платеж
**Цель:** освоить многострочные строки `"""..."""`.  
**Задача:** Сформируйте JSON (как строку) через text block и подставьте значения через `.formatted(...)`:
- `clientId` (int)
- `amount` (double, 2 знака после запятой)
- `currency` (String)

Пример формы:
```json
{
  "clientId": 10,
  "amount": 99.90,
  "currency": "USD"
}
```

**Контекст:** в реальном процессинге часто собирают JSON для внешних API (пока что — просто строка).

---

### Мини-шаблон
Можно все задания делать в одном `main`, по очереди печатая результаты.

```java
public class Main {
    public static void main(String[] args) {
        // TODO: решай задания 1..10, печатай результаты через System.out.println(...)
    }
}
```

## <a name="livecode2"></a> LIVE CODING. ЧАСТЬ 2. Массивы

### Задание 1. Пакет транзакций за минуту (инициализация массива + `length`)
**Цель:** объявление и инициализация массива примитивов, понимание `length`.  
**Задача:** Создайте массив `int[] amountsInCents` из 5 платежей (в центах), например: `{1000, 2500, 499, 100_00, 75_50}`.  
Выведите:
- размер массива через `amountsInCents.length`
- первый и последний элемент

**Контекст:** процессинг получил 5 платежей за минуту.

---

### Задание 2. Заполнение массива значениями по умолчанию (и почему это важно)
**Цель:** понять дефолтные значения в массивах.  
**Задача:** Создайте:
- `double[] fxRates = new double[3];`
- `String[] currencies = new String[3];`
- `boolean[] fraudFlags = new boolean[3];`

Выведите эти массивы через `Arrays.toString(...)`.  
Объясните в комментарии: почему в `currencies` будут `null`, а в `fxRates` — `0.0`.

---

### Задание 3. Безопасный доступ по индексу (демо `ArrayIndexOutOfBoundsException`)
**Цель:** закрепить индексацию и типичную ошибку.  
**Задача:** Дано:
- `String[] statuses = {"INIT", "AUTH", "CAPTURE", "COMPLETED"};`

1) Выведите `statuses[3]`  
2) Затем попробуйте обратиться к `statuses[4]` и посмотрите исключение  
3) Исправьте код: обращайтесь к «последнему элементу» через `statuses[statuses.length - 1]`

---

### Задание 4. Изменение элемента массива: корректировка суммы платежа
**Цель:** научиться менять элемент массива.  
**Задача:** Дано:
- `int[] amounts = {1000, 2000, 3000};`

Представьте, что второй платеж (индекс 1) скорректировали комиссией +100.  
Измените `amounts[1]` и выведите массив до/после.

---

### Задание 5. Подсчет дневного объема (цикл `for-each`)
**Цель:** отработать обход массива без индекса.  
**Задача:** Дано:
- `int[] amounts = {1500, 4999, 1200, 3050};`

Посчитайте сумму `total` через `for-each` и выведите:
- `total` в центах
- (опционально) строку вида `Total: 87.49 USD` через `String.format`, разделив центы на 100.0

---

### Задание 6. Когда нужен индекс: построение таблицы «номер платежа → сумма» (классический `for`)
**Цель:** понять пользу индекса.  
**Задача:** Для массива `amounts` выведите строки:
- `payment[0] = 1500`
- `payment[1] = 4999`
и т.д.

**Контекст:** лог процессинга с номером операции в батче.

---

### Задание 7. Массивы объектов: коды валют и нормализация (String[])
**Цель:** массив ссылочных типов + работа со строками из лекции 3.  
**Задача:** Дано:
- `String[] raw = {" usd", "EUR ", "  rUb  "};`

Создайте новый массив `String[] normalized` той же длины и заполните его значениями:
- `strip()` (или `trim()`) + `toUpperCase()`

Выведите оба массива через `Arrays.toString(...)`.

---

### Задание 8. `Arrays.sort(...)`: сортировка сумм для антифрода
**Цель:** применить `Arrays.sort` и понять, что массив меняется.  
**Задача:** Дано:
- `int[] amounts = {5000, 100, 2500, 2500, 9999};`

1) Отсортируйте массив по возрастанию  
2) Выведите результат  
3) Выведите минимальную и максимальную сумму (после сортировки — это первый/последний элемент)

**Контекст:** быстрый анализ распределения сумм.

---

### Задание 9. Копирование и «срезы»: `copyOf` и `copyOfRange`
**Цель:** научиться копировать массивы (а не ссылку).  
**Задача:** Дано:
- `int[] day1 = {100, 200, 300, 400, 500};`

Сделайте:
1) `int[] copyAll = Arrays.copyOf(day1, day1.length);`  
2) `int[] first3 = Arrays.copyOf(day1, 3);`  
3) `int[] middle = Arrays.copyOfRange(day1, 1, 4);` (правая граница не включается!)

Выведите все массивы.

---

### Задание 10. Varargs как массив: суммарная комиссия по операциям
**Цель:** понять varargs и цикл по массиву аргументов.  
**Задача:** Напишите метод:

- `static void totalFee(int... feesInCents)` — выводит все элементы массива feesInCents.

Проверьте вызовы:
- `totalFee(10, 20, 30)`
- `totalFee()` (ничего не будет выведено)

**Контекст:** комиссии могут приходить списком разной длины.

---

### Мини-шаблон для запуска (с импортами)
```java
import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        // TODO: решай задания 1..9
    }

    static void totalFee(int... feesInCents) {
        цикл + вывод
    }
}
```


---

## <a name="homework"></a> 🏠 СКВОЗНАЯ РАБОТА № 3

### Задание: Рефакторинг кода с использованием массивов и улучшенной работы со строками

На лекции №3 вы изучили **строки и массивы** в Java. В этом задании вы **рефакторите** код из Сквозной работы №2, заменив отдельные переменные для каждого платежа на **массивы**, что сделает код более компактным и масштабируемым.

Вы также добавите новую функциональность для работы со строками: форматирование имен клиентов, валидацию данных и генерацию отчетов.

---

### Требования

#### 1. Замена отдельных переменных на массивы

Возьмите данные из переменных для платежей (`amount1`, `currency1`, `status1`, `clientName1` и т.д.) и замените их на **массивы**:

```java
// Массивы для хранения данных о платежах
long[] amounts = {заполните данными};
String[] currencies = {заполните данными};
String[] statuses = {заполните данными};
String[] clientNames = {заполните данными};
```
Старые переменные нужно удалить.  

#### 2. Упрощение цикла обработки платежей

Измените цикл `for` так, чтобы он напрямую работал с массивами:

```java
for (проход по amounts) {
    long amount = amounts[i];
    String currency = currencies[i];
    String status = statuses[i];
    String clientName = clientNames[i];
    
    // ... остальная логика обработки
}
```

Удалите блок с `if-else` для выбора данных платежа — теперь данные берутся напрямую из массивов.

#### 3. Добавление обработки имен клиентов

Перед выводом информации о платеже добавьте обработку имени клиента:

- Проверьте, что имя клиента не `null` и не пустая строка.
- Если имя содержит только пробелы, используйте метод `trim()` для очистки.
- Преобразуйте имя клиента в верхний регистр для вывода в отчете.
- Извлеките **инициалы** клиента (первые буквы имени и фамилии). Например, для "Иван Иванов" → "И.И."
- Выведите фамилию клиента + инициалы

#### 4. Создание массива для хранения комиссий

Создайте массив `long[] fees` для хранения рассчитанных комиссий:

```java
long[] fees = new long[amounts.length];
```

Сохраняйте рассчитанную комиссию в этот массив на каждой итерации:

#### 5. Генерация итогового отчета

После завершения цикла обработки платежей добавьте блок для генерации итогового отчета с использованием цикла `for-each`:

```java
System.out.println("\n=== ИТОГОВЫЙ ОТЧЕТ ===");
long totalFees = 0;

for (проход по fees) {
    if (fees[i] > 0) {
        System.out.println("Платеж " + (i + 1) + ": комиссия=" + fees[i] + " " + currencies[i]);
        totalFees += fees[i];
    }
}

System.out.println("Общая сумма комиссий: " + totalFees);
```

#### 6. Добавление валидации валюты

Создайте массив допустимых валют:

```java
String[] validCurrencies = {"RUB", "USD", "EUR"};
```

Перед расчетом комиссии проверьте, что валюта платежа присутствует в массиве `validCurrencies`. Если валюта недопустима, выведите сообщение об ошибке и используйте `continue`.

Пример проверки:

```java
boolean isValidCurrency = false;
for (проход по validCurrencies) {
    if (validCurrency равен currency) {
        isValidCurrency = true;
        break;
    }
}

if (!isValidCurrency) {
    System.out.println("Ошибка: недопустимая валюта для платежа " + (i + 1));
    continue;
}
```

#### 7. Использование `var`

Используйте `var` минимум в **3 местах**, где тип переменной очевиден (например, при объявлении массивов, в циклах, для строковых переменных).

#### 8. Демонстрация работы с `null` в массивах (закомментировано)

В конце метода `main` добавьте **закомментированный** блок кода, демонстрирующий проблему с `NullPointerException` при работе с массивами строк.

#### 9. Качество кода

- Запустите `mvn clean compile` — код должен компилироваться без ошибок.
- Запустите `mvn checkstyle:check` — исправьте все замечания.
- Соблюдайте naming conventions (camelCase для переменных, UPPER_CASE для констант).
- Сохраните всю логику из Сквозной работы №2 (проверки на `null`, `FAILED`, нулевую сумму, расчет комиссий с минимумом).

---

### Пример вывода программы:

```
=== Payment Processing System ===
Начало обработки платежей...

Платеж 1: клиент=ИВАНОВ И.И., сумма=150000 RUB, статус=NEW, комиссия=1500
Платеж 2: клиент=ПЕТРОВ П.П., сумма=1000 USD, статус=NEW, комиссия=50
Платеж 3 отклонен (статус FAILED)
Платеж 4: клиент=ИВАНОВА М.И., сумма=200000 EUR, статус=COMPLETED, комиссия=0
Обнаружен платеж с нулевой суммой. Остановка обработки.

=== ИТОГОВЫЙ ОТЧЕТ ===
Платеж 1: комиссия=1500 RUB
Платеж 2: комиссия=50 USD
Общая сумма комиссий: 1550

Всего обработано платежей: 3
=== Обработка завершена ===
```

### 🎓 Методические советы студенту:
1.  **Массивы:** Помни, что индексы в Java начинаются с **0**, а последний элемент имеет индекс `length - 1`.
2.  **Строки:** Метод `split(" ")` возвращает массив строк. Если в строке "Иван Иванов", то `parts[0]` — это "Иван", а `parts[1]` — "Иванов". *Внимательно посмотри, что ты берешь для фамилии, а что для инициалов.*
3.  **Типы данных:** При расчете комиссии помни про целочисленное деление. Если нужно сохранить точность, убедись, что работаешь с типом `long`.
4.  **Java 21:** Не забудь использовать `var` там, где тип данных понятен из правой части выражения (например, при создании массивов).

---

### Критерии оценки

- ✅ Отдельные переменные для платежей заменены на массивы.
- ✅ Цикл обработки упрощен и работает напрямую с массивами.
- ✅ Реализована обработка имен клиентов (проверка на пустоту, удалеие пробелов, приведение к верхнему регистру, извлечение инициалов).
- ✅ Создан массив для хранения комиссий, комиссии сохраняются в него.
- ✅ Реализована генерация итогового отчета с подсчетом общей суммы комиссий.
- ✅ Добавлена валидация валюты с использованием массива допустимых значений.
- ✅ Использовано ключевое слово `var` минимум в 3 местах.
- ✅ Есть закомментированный пример, демонстрирующий `NullPointerException` при работе с массивами.
- ✅ Вся логика из Сквозной работы №2 сохранена (проверки, расчеты, операторы `break`/`continue`).
- ✅ Код компилируется и проходит проверку Checkstyle без ошибок.
- ✅ Изменения оформлены в отдельном PR в GitHub.


### Подсказка на псевдокоде
В репозитории есть файл homeworkN_pseudocode.java. Это выполненное сквозное задание с использованием псевдокода.  
**Рекомендации**: попробовать сделать задание без него. Если вы застряли или потратили много времени воспользуйтесь им.
